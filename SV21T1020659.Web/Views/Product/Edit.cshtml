@* @{
}
<div class="box box-primary">

    <div class="box-header with-border ">
        <h3 class="box-title">Thông tin mặt hàng</h3>
        <div class="box-tools pull-right">
            <button type="button" class="btn btn-box-tool" data-widget="collapse" data-toggle="tooltip" title="" data-original-title="Collapse">
                <i class="fa fa-minus"></i>
            </button>
        </div>
    </div>

    <div class="box-body">
        <form class="form-horizontal" action="#" method="post" enctype="multipart/form-data">

            <div class="form-group">
                <label class="control-label col-sm-2">Tên mặt hàng:</label>
                <div class="col-sm-10">
                    <input type="text" class="form-control" autofocus name="ProductName">
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Mô tả:</label>
                <div class="col-sm-10">
                    <textarea class="form-control" rows="3" name="ProductDescription"></textarea>
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Loại hàng:</label>
                <div class="col-sm-10">
                    <select class="form-control" name="CategoryId">
                        <option>-- Loại hàng --</option>
                        <option>Mỹ phẩm</option>
                        <option>Thực phẩm</option>
                    </select>
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Nhà cung cấp:</label>
                <div class="col-sm-10">
                    <select class="form-control" name="SupplierID">
                        <option>-- Nhà cung cấp --</option>
                        <option>Công ty TNHH Thành Công</option>
                        <option>Vina Foods</option>
                    </select>
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Đơn vị tính:</label>
                <div class="col-sm-10">
                    <input type="text" class="form-control" name="Unit">
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Giá hàng:</label>
                <div class="col-sm-10">
                    <input type="text" class="form-control" name="Price">
                </div>
            </div>

            <div class="form-group">
                <div class="col-lg-offset-2 col-sm-10">
                    <div class="checkbox">
                        <label><input type="checkbox" value="true" checked name="IsSelling">Hiện đang được bán</label>
                    </div>
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Ảnh minh họa:</label>
                <div class="col-sm-10">
                    <input type="file" class="form-control" name="uploadPhoto" accept="image/*"
                           onchange="document.getElementById('Photo').src = window.URL.createObjectURL(this.files[0])" />
                </div>
            </div>

            <div class="form-group">
                <div class="col-lg-offset-2 col-sm-10">
                    <input type="hidden" name="Photo" value="macbook.png" />
                    <img id="Photo" src="~/images/products/macbook.png" class="img img-bordered" style="width:200px" />
                </div>
            </div>

            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <button type="submit" class="btn btn-primary">
                        <i class="fa fa-save"></i> Lưu dữ liệu
                    </button>
                    <a class="btn btn-default" href="~/Product">
                        Quay lại
                    </a>
                </div>
            </div>
        </form>

    </div>

</div>


<div class="box box-info">
    <div class="box-header with-border ">
        <h3 class="box-title">Thư viện ảnh</h3>
        <div class="box-tools pull-right">
            <button type="button" class="btn btn-box-tool" data-widget="collapse" data-toggle="tooltip" title="" data-original-title="Collapse">
                <i class="fa fa-minus"></i>
            </button>
        </div>
    </div>
    <div class="box-body">
        <table class="table table-bordered table-hover table-striped">
            <thead>
                <tr class="bg-gray">
                    <th>Ảnh</th>
                    <th>Mô tả/tiêu đề</th>
                    <th>Thứ tự hiển thị</th>
                    <th>Ẩn ảnh</th>
                    <th class="text-right">
                        <a class="btn btn-xs btn-primary" href="~/Product/Photo/123?method=add">
                            <i class="fa fa-plus"></i>
                        </a>
                    </th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 1; i <= 3; i++)
                {
                    <tr>
                        <td style="vertical-align:middle">
                            <img src="~/Images/Products/macbook.png" alt="MacBookPro" style="width:100px" />
                        </td>
                        <td style="vertical-align:middle;">
                            MacBook Pro M2 15" - 2023
                        </td>
                        <td style="vertical-align:middle;">@i</td>
                        <td style="vertical-align:middle;"></td>
                        <td style="vertical-align:middle; text-align:right">
                            <a class="btn btn-xs btn-primary" href="~/Product/Photo/123?method=edit&photoId=456">
                                <i class="fa fa-edit"></i>
                            </a>
                            <a class="btn btn-xs btn-danger" href="~/Product/Photo/123?method=delete&photoId=456" onclick="return confirm('Xóa ảnh của mặt hàng hay không?')">
                                <i class="fa fa-trash"></i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

</div>

<div class="box box-info">

    <div class="box-header with-border ">
        <h3 class="box-title">Thuộc tính của mặt hàng</h3>
        <div class="box-tools pull-right">
            <button type="button" class="btn btn-box-tool" data-widget="collapse" data-toggle="tooltip" title="" data-original-title="Collapse">
                <i class="fa fa-minus"></i>
            </button>
        </div>
    </div>

    <div class="box-body">
        <table class="table table-bordered table-hover table-striped">
            <thead>
                <tr class="bg-gray">
                    <th>Tên thuộc tính</th>
                    <th>Giá trị thuộc tính</th>
                    <th>Thứ tự hiển thị</th>
                    <th class="text-right">
                        <a class="btn btn-xs btn-primary" href="~/Product/Attribute/123?method=add">
                            <i class="fa fa-plus"></i>
                        </a>
                    </th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 1; i <= 5; i++)
                {
                    <tr>
                        <td style="vertical-align:middle">CPU</td>
                        <td style="vertical-align:middle;">Intel Core i5 3337U 1.8GHz</td>
                        <td style="vertical-align:middle;">1</td>
                        <td style="vertical-align:middle; text-align:right">
                            <a class="btn btn-xs btn-primary" href="~/Product/Attribute/123?method=edit&attributeId=456">
                                <i class="fa fa-edit"></i>
                            </a>
                            <a class="btn btn-xs btn-danger" href="~/Product/Attribute/123?method=delete&attributeId=456" onclick="return confirm('Xóa thuộc tính này của mặt hàng?')">
                                <i class="fa fa-trash"></i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

 *@

 @model SV21T1020659.DomainModels.Product

<div class="box box-primary">
    <div class="box-header with-border ">
        <h3 class="box-title">Thông tin mặt hàng</h3>
        <div class="box-tools pull-right">
            <button type="button" class="btn btn-box-tool" data-widget="collapse" data-toggle="tooltip" title="" data-original-title="Collapse">
                <i class="fa fa-minus"></i>
            </button>
        </div>
    </div>

    <div class="box-body">
        <form class="form-horizontal" action="~/Product/Save" method="post" enctype="multipart/form-data">
            <input type="hidden" name="ProductID" value="@Model.ProductID" />  <!-- Thêm dòng này -->
            <div class="form-group">
                <label class="control-label col-sm-2">Tên mặt hàng:</label>
                <div class="col-sm-10">
                    <input type="text" class="form-control" autofocus name="ProductName" value="@Model.ProductName">
                    @Html.ValidationMessage(nameof(Model.ProductName))
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Mô tả:</label>
                <div class="col-sm-10">
                    <textarea class="form-control" rows="3" name="ProductDescription">@Model.ProductDescription</textarea>
                    @Html.ValidationMessage(nameof(Model.ProductDescription))
                </div>
            </div>
            <div class="form-group">
                <label class="control-label col-sm-2">Loại hàng:</label>
                <div class="col-sm-10">
                    <select class="form-control" name="@nameof(Model.CategoryID)">
                        <option value="0">-- Chọn loại hàng --</option>
                        @foreach (var item in CommonDataService.ListOfCategories())
                        {
                            if (item.CategoryID == Model.CategoryID)
                            {
                                <option value="@item.CategoryID" selected>
                                    @item.CategoryName
                                </option>
                            }
                            else
                            {
                                <option value="@item.CategoryID">
                                    @item.CategoryName
                                </option>

                            }
                        }
                    </select>
                    @Html.ValidationMessageFor(model => model.CategoryID)
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Nhà cung cấp:</label>
                <div class="col-sm-10">
                    <select class="form-control" name="@nameof(Model.SupplierID)">
                        <option value="0">-- Chọn nhà cung cấp --</option>
                        @foreach (var item in CommonDataService.ListOfSuppliers())
                        {
                            if (item.SupplierID == Model.SupplierID)
                            {
                                <option value="@item.SupplierID" selected>
                                    @item.SupplierName
                                </option>
                            }
                            else
                            {
                                <option value="@item.SupplierID">
                                    @item.SupplierName
                                </option>
                            }
                        }
                    </select>
                    @Html.ValidationMessage(nameof(Model.SupplierID))
                </div>
            </div>




            <div class="form-group">
                <label class="control-label col-sm-2">Đơn vị tính:</label>
                <div class="col-sm-10">
                    <input type="text" class="form-control" name="Unit" value="@Model.Unit">
                    @Html.ValidationMessage(nameof(Model.Unit))
                </div>

            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Giá hàng:</label>
                <div class="col-sm-10">
                    <input type="text" class="form-control" name="Price" value="@Model.Price.ToString("N0")">
                    @Html.ValidationMessage(nameof(Model.Price))
                </div>
            </div>


            <div class="form-group">
                <div class="col-lg-offset-2 col-sm-10">
                    <div class="checkbox">
                        <label>
                            <input type="checkbox" value="true" @(Model.IsSelling ? "checked" : "") name="IsSelling">Hiện đang được bán
                        </label>
                    </div>
                </div>
            </div>

            <div class="form-group">
                <label class="control-label col-sm-2">Ảnh minh họa:</label>
                <div class="col-sm-10">
                    <input type="file" class="form-control" name="uploadPhoto" accept="image/*"
                           onchange="document.getElementById('PhotoPreview').src = window.URL.createObjectURL(this.files[0])" />
                </div>
            </div>

            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <input type="hidden" name="Photo" value="@Model.Photo" />
                    <img id="PhotoPreview" src="~/images/products/@Model.Photo" class="img img-bordered" style="width:200px" />
                </div>
            </div>


            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <button type="submit" class="btn btn-primary">
                        <i class="fa fa-save"></i> Lưu dữ liệu
                    </button>
                    <a class="btn btn-default" href="~/Product">
                        Quay lại
                    </a>
                </div>
            </div>
        </form>
    </div>
</div>
@if (Model.ProductID > 0) {
<div class="box box-info">
    <div class="box-header with-border ">
        <h3 class="box-title">Thư viện ảnh</h3>
        <div class="box-tools pull-right">
            <button type="button" class="btn btn-box-tool" data-widget="collapse" data-toggle="tooltip" title="" data-original-title="Collapse">
                <i class="fa fa-minus"></i>
            </button>
        </div>
    </div>
    <div class="box-body">
        <table class="table table-bordered table-hover table-striped">
            <thead>
                <tr class="bg-gray">
                    <th>Ảnh</th>
                    <th>Mô tả/tiêu đề</th>
                    <th>Thứ tự hiển thị</th>
                    <th>Ẩn ảnh</th>
                    <th class="text-right">
                        <a class="btn btn-xs btn-primary" href="@Url.Action("Photo", "Product", new { id = Model.ProductID, method = "add" })">
                            <i class="fa fa-plus"></i>
                        </a>
                    </th>
                </tr>
            </thead>
            <tbody>
               @*  @if (ViewBag.Photos != null && ViewBag.Photos.Count > 0)
                {
                    foreach (var photo in ViewBag.Photos)
                    {
                        <tr>
                            <td style="vertical-align:middle">
                                <img src="~/images/products/@photo.Photo" alt="@photo.Description" style="width:70px; height:70px; object-fit:cover;" />
                            </td>
                            <td style="vertical-align:middle;">
                                @photo.Description
                            </td>
                            <td style="vertical-align:middle;">@photo.DisplayOrder</td>
                            <td style="vertical-align:middle;">
                                <input type="checkbox" @(photo.IsHidden ? "checked" : "") disabled />
                            </td>
                            <td style="vertical-align:middle; text-align:right">
                                <a class="btn btn-xs btn-primary" href="@Url.Action("Photo", "Product", new { id = Model.ProductID, method = "edit", photoId = photo.PhotoID })">
                                    <i class="fa fa-edit"></i>
                                </a>
                                <a class="btn btn-xs btn-danger" href="@Url.Action("Photo", "Product", new { id = Model.ProductID, method = "delete", photoId = photo.PhotoID })" onclick="return confirm('Xóa ảnh của mặt hàng hay không?')">
                                    <i class="fa fa-trash"></i>
                                </a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="5" class="text-center">Không có ảnh nào được thêm.</td>
                    </tr>
                } *@
                    @if (ViewBag.Photos != null && ViewBag.Photos.Count > 0)
                    {
                        foreach (var photo in ViewBag.Photos)
                        {
                            <tr>
                                <td style="vertical-align:middle">
                                    @if (photo.IsHidden)
                                    {
                                        <img src="~/images/products/noproduct.jpg" alt="No Product Image" style="width:70px; height:70px; object-fit:cover;" />
                                    }
                                    else
                                    {
                                        <img src="~/images/products/@photo.Photo" alt="@photo.Description" style="width:70px; height:70px; object-fit:cover;" />
                                    }
                                </td>
                                <td style="vertical-align:middle;">
                                    @photo.Description
                                </td>
                                <td style="vertical-align:middle;">@photo.DisplayOrder</td>
                                <td style="vertical-align:middle;">
                                    <input type="checkbox" @(photo.IsHidden ? "checked" : "") disabled />
                                </td>
                                <td style="vertical-align:middle; text-align:right">
                                    <a class="btn btn-xs btn-primary" href="@Url.Action("Photo", "Product", new { id = Model.ProductID, method = "edit", photoId = photo.PhotoID })">
                                        <i class="fa fa-edit"></i>
                                    </a>
                                    <a class="btn btn-xs btn-danger" href="@Url.Action("Photo", "Product", new { id = Model.ProductID, method = "delete", photoId = photo.PhotoID })" onclick="return confirm('Xóa ảnh của mặt hàng hay không?')">
                                        <i class="fa fa-trash"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" class="text-center">Không có ảnh nào được thêm.</td>
                        </tr>
                    }

            </tbody>
        </table>
    </div>
</div>


<div class="box-body">
    <table class="table table-bordered table-hover table-striped">
        <thead>
            <tr class="bg-gray">
                <th>Tên thuộc tính</th>
                <th>Giá trị thuộc tính</th>
                <th>Thứ tự hiển thị</th>
                <th class="text-right">
                    <a class="btn btn-xs btn-primary" href="@Url.Action("Attribute", "Product", new { id = Model.ProductID, method = "add" })">
                        <i class="fa fa-plus"></i>
                    </a>
                </th>
            </tr>
        </thead>
        <tbody>
            @if (ViewBag.Attributes != null && ViewBag.Attributes.Count > 0)
            {
                foreach (var attr in ViewBag.Attributes)
                {
                    <tr>
                        <td style="vertical-align:middle">@attr.AttributeName</td>
                        <td style="vertical-align:middle;">@attr.AttributeValue</td>
                        <td style="vertical-align:middle;">@attr.DisplayOrder</td>
                        <td style="vertical-align:middle; text-align:right">
                            <a class="btn btn-xs btn-primary" href="@Url.Action("Attribute", "Product", new { id = Model.ProductID, method = "edit", attributeId = attr.AttributeID })">
                                <i class="fa fa-edit"></i>
                            </a>
                            <a class="btn btn-xs btn-danger" href="@Url.Action("Attribute", "Product", new { id = Model.ProductID, method = "delete", attributeId = attr.AttributeID })" onclick="return confirm('Xóa thuộc tính của mặt hàng hay không?')">
                                <i class="fa fa-trash"></i>
                            </a>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="4" class="text-center">Không có thuộc tính nào được thêm.</td>
                </tr>
            }
        </tbody>
    </table>
</div>
}
</div>